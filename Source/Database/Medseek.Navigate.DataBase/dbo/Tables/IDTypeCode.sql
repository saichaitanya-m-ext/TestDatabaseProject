CREATE TABLE [dbo].[IDTypeCode] (
    [IDTypeCodeID] INT           IDENTITY (1, 1) NOT NULL,
    [Value]        VARCHAR (100) NULL,
    CONSTRAINT [PK_IDTypeCode] PRIMARY KEY CLUSTERED ([IDTypeCodeID] ASC)
);

